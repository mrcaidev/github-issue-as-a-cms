---
import { fetchLatestTags } from "utils/post/fetchLatestTags";
import ArrowRight from "./icons/ArrowRight";
import SectionTitle from "./SectionTitle.astro";
import Tag from "./Tag.astro";

const latestTags = await fetchLatestTags();
---

<div class="flex justify-between items-center px-2">
  <SectionTitle>🏷️ Tags</SectionTitle>
  <a
    href="/tags"
    class="flex items-center gap-1 px-2 py-1 text-sm md:text-base hover:text-cyan-11 dark:hover:text-cyandark-11"
  >
    View all
    <ArrowRight size={16} />
  </a>
</div>
<ul class="columns-xs gap-4 mt-4">
  {
    latestTags.map((tag) => (
      <li>
        <Tag ghost {...tag} />
      </li>
    ))
  }
</ul>
